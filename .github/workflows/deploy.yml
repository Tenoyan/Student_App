# name: Build, Push Docker & Deploy

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Log in to Docker Hub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build React image
#         run: docker build -f student-frontend/Dockerfile -t ${{ secrets.DOCKERHUB_USERNAME }}/react_app:latest student-frontend

#       - name: Build Laravel image
#         run: docker build -f student-app/Dockerfile -t ${{ secrets.DOCKERHUB_USERNAME }}/laravel_app:latest student-app

#       - name: Build MySQL image
#         run: docker build -f student-mysql/Dockerfile -t ${{ secrets.DOCKERHUB_USERNAME }}/mysql_app:latest student-mysql

#       - name: Push MySQL image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/mysql_app:latest


#       - name: Push React image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/react_app:latest

#       - name: Push Laravel image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/laravel_app:latest

#       - name: Deploy to Server via SSH
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.SERVER_IP }}
#           username: ${{ secrets.SERVER_USER }}
#           key: ${{ secrets.SERVER_SSH_KEY }}
#           script: |
#             cd ${{ secrets.SERVER_PATH }}
#             git reset --hard
#             git clean -fd
#             git pull origin main
#             docker pull ${{ secrets.DOCKERHUB_USERNAME }}/react_app:latest
#             docker pull ${{ secrets.DOCKERHUB_USERNAME }}/laravel_app:latest
#             docker pull mysql:8.0
#             docker compose down
#             docker compose up -d


# 
# 
# 
# 
# 


# name: Build, Push Docker Images & Deploy

# on:
#   push:
#     branches:
#       - Ten

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout source code
#         uses: actions/checkout@v4

#       - name: Login to Docker Hub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build React image
#         run: |
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/react_app:latest -f student-frontend/Dockerfile student-frontend

#       - name: Build Laravel image
#         run: |
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/laravel_app:latest -f student-app/Dockerfile student-app

#       - name: Build MySQL image
#         run: docker build -f student-mysql/Dockerfile -t ${{ secrets.DOCKERHUB_USERNAME }}/mysql_app:latest student-mysql

#       - name: Push MySQL image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/mysql_app:latest


#       - name: Push React image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/react_app:latest

#       - name: Push Laravel image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/laravel_app:latest

#       - name: Deploy to Server
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.SERVER_IP }}
#           username: ${{ secrets.SERVER_USER }}
#           key: ${{ secrets.SERVER_SSH_KEY }}
#           script: |
#             set -e
#             cd ${{ secrets.SERVER_PATH }}
#             git pull origin Ten
#             docker compose pull --no-build
#             docker compose down
#             docker compose up -d


# name: Build, Push Docker Images & Deploy

# on:
#   push:
#     branches:
#       - Ten

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout source code
#         uses: actions/checkout@v4

#       - name: Login to Docker Hub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build React image
#         run: |
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/react_app:latest \
#           -f student-frontend/Dockerfile student-frontend

#       - name: Build Laravel image
#         run: |
#           docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/laravel_app:latest \
#           -f student-app/Dockerfile student-app

#       - name: Build MySQL image
#         run: docker build -f student-mysql/Dockerfile -t ${{ secrets.DOCKERHUB_USERNAME }}/mysql_app:latest student-mysql

#       - name: Push MySQL image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/mysql_app:latest


#       - name: Push React image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/react_app:latest

#       - name: Push Laravel image
#         run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/laravel_app:latest

#       - name: Deploy to Server
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           # host: ${{ secrets.SERVER_IP }}
#           # username: ${{ secrets.SERVER_USER }}
#           # key: ${{ secrets.SERVER_SSH_KEY }}
#           script: |
#             set -e
            

#             git pull origin Ten

#             docker compose pull
#             docker compose down
#             docker compose up -d

#             docker exec laravel php artisan key:generate --force || true
#             docker exec laravel php artisan migrate --force
#             docker exec laravel php artisan config:clear
#             docker exec laravel php artisan cache:clear



name: Build, Test & Run Containers in CI/CD

on:
  push:
    branches:
      - main
      - Ten
  pull_request:
    branches:
      - main
      - Ten
  workflow_dispatch:  # Allow manual triggering

env:
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}

jobs:
  build-test-run:
    runs-on: self-hosted  # Will use self-hosted if available, otherwise GitHub-hosted
    
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Create .env file for testing
        working-directory: ./student-app
        shell: powershell
        run: |
          Set-Content -Path ".\.env" -Value @"
          APP_NAME=Laravel
          APP_ENV=testing
          APP_KEY=
          APP_DEBUG=true
          APP_TIMEZONE=UTC
          APP_URL=http://localhost
          
          DB_CONNECTION=mysql
          DB_HOST=sql_db
          DB_PORT=3306
          DB_DATABASE=student_app
          DB_USERNAME=root
          DB_PASSWORD=Sumo@3396
          
          SESSION_DRIVER=database
          QUEUE_CONNECTION=database
          CACHE_STORE=database
          BROADCAST_CONNECTION=log
          FILESYSTEM_DISK=local
          "@

      # - name: Build React image
      #   run: |
      #     docker build -t ${{ env.DOCKERHUB_USERNAME }}/react_app:latest \
      #     -f student-frontend/Dockerfile student-frontend
      #     # Tag with the name docker-compose.yml expects
      #     docker tag ${{ env.DOCKERHUB_USERNAME }}/react_app:latest nktenoyan/react_app:latest

      - name: Build React image
        working-directory: ${{ github.workspace }}
        shell: powershell
        run: |
          docker build -t $env:DOCKERHUB_USERNAME/react_app:latest -f .\student-frontend\Dockerfile .\student-frontend
          docker tag $env:DOCKERHUB_USERNAME/react_app:latest $env:DOCKERHUB_USERNAME/react_app:latest


      # - name: Build Laravel image
      #   run: |
      #     docker build -t ${{ env.DOCKERHUB_USERNAME }}/laravel_app:latest \
      #     -f student-app/Dockerfile student-app
      #     # Tag with the name docker-compose.yml expects
      #     docker tag ${{ env.DOCKERHUB_USERNAME }}/laravel_app:latest nktenoyan/laravel_app:latest

      - name: Build Laravel image
        working-directory: ${{ github.workspace }}
        shell: powershell
        run: |
          docker build -t $env:DOCKERHUB_USERNAME/laravel_app:latest -f .\student-app\Dockerfile .\student-app
          docker tag $env:DOCKERHUB_USERNAME/laravel_app:latest $env:DOCKERHUB_USERNAME/laravel_app:latest


      # - name: Build MySQL image
      #   run: |
      #     docker build -t ${{ env.DOCKERHUB_USERNAME }}/mysql_app:latest \
      #     -f student-mysql/Dockerfile student-mysql
      #     # Tag with the name docker-compose.yml expects
      #     docker tag ${{ env.DOCKERHUB_USERNAME }}/mysql_app:latest nktenoyan/mysql_app:latest

      - name: Build MySQL image
        working-directory: ${{ github.workspace }}
        shell: powershell
        run: |
          docker build -t $env:DOCKERHUB_USERNAME/mysql_app:latest -f .\student-mysql\Dockerfile .\student-mysql
          docker tag $env:DOCKERHUB_USERNAME/mysql_app:latest $env:DOCKERHUB_USERNAME/mysql_app:latest


      - name: Run containers automatically with docker-compose stack first-application
        run: |
          Write-Host '=========================================='
          Write-Host 'AUTOMATIC CONTAINER STARTUP - CI/CD Pipeline'
          Write-Host 'Stack Name: first-application'

          # Verify images
          Write-Host 'Verifying images...'
          docker images | Select-String "react_app|laravel_app|mysql_app"

          # Start containers
          Write-Host 'Starting containers...'
          try {
              docker compose -p first-application up -d
              Write-Host '✓ Containers started successfully'
          } catch {
              Write-Host 'Failed to start containers'
              docker compose -p first-application logs
              exit 1
          }

          Start-Sleep -Seconds 5

          # Show container status
          docker ps -a

          # Wait for MySQL
          Write-Host 'Waiting for MySQL...'
          $maxRetries = 60
          $retry = 0
          while ($retry -lt $maxRetries) {
              try {
                  docker exec sql_db mysqladmin ping -h localhost | Out-Null
                  Write-Host 'MySQL is ready!'
                  break
              } catch {
                  Start-Sleep -Seconds 2
                  $retry++
                  if ($retry -eq $maxRetries) {
                      Write-Host 'MySQL failed to start'
                      docker logs sql_db
                      exit 1
                  }
              }
          }

          # Laravel setup
          docker exec laravels php artisan key:generate --force
          docker exec laravels php artisan config:clear
          docker exec laravels php artisan cache:clear

          # Run migrations
          try {
              docker exec laravels php artisan migrate --force
              Write-Host 'Migrations completed successfully!'
          } catch {
              Write-Host 'Migration failed'
              docker logs laravels
              exit 1
          }

          # Test database connection
          try {
              docker exec laravels php artisan tinker --execute='DB::connection()->getPdo(); Write-Host "Database connection successful!"'
          } catch {
              Write-Host 'Database connection test failed'
              exit 1
          }

          # Test services
          Start-Sleep -Seconds 5
          try {
              $reactResponse = Invoke-WebRequest -Uri 'http://localhost:80' -UseBasicParsing
              Write-Host ('React app status: ' + $reactResponse.StatusCode)
          } catch {
              Write-Host 'React app not responding'
          }

          Start-Sleep -Seconds 5
          try {
              $laravelResponse = Invoke-WebRequest -Uri 'http://localhost:8000' -UseBasicParsing
              Write-Host ('Laravel API status: ' + $laravelResponse.StatusCode)
          } catch {
              Write-Host 'Laravel API not responding'
          }

          # Final container status
          docker ps
          Write-Host 'All containers are running successfully in stack first-application!'



      - name: Run container health checks
        run: |
          echo "Running health checks..."
          
          # Check MySQL health
          docker exec sql_db mysqladmin ping -h localhost --silent && echo "✓ MySQL is healthy" || echo "✗ MySQL health check failed"
          
          # Check Laravel container
          docker exec laravels php artisan --version && echo "✓ Laravel is working" || echo "✗ Laravel check failed"
          
          # Check if containers can communicate
          docker exec laravels php artisan tinker --execute="try { DB::table('users')->count(); echo '✓ Database query successful'; } catch (Exception \$e) { echo '✗ Database query failed: ' . \$e->getMessage(); }"

      - name: Show container logs
        if: failure()
        run: |
          echo "=== MySQL Logs ==="
          docker logs sql_db --tail 50
          echo "=== Laravel Logs ==="
          docker logs laravels --tail 50
          echo "=== React Logs ==="
          docker logs reacts --tail 50
          echo "=== Docker Compose Stack Status ==="
          docker compose -p first-application ps

      - name: Login to Docker Hub
        if: github.event_name == 'push'
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push React image to Docker Hub
        if: github.event_name == 'push'
        run: |
          docker push ${{ env.DOCKERHUB_USERNAME }}/react_app:latest
          echo "React image pushed successfully"

      - name: Push Laravel image to Docker Hub
        if: github.event_name == 'push'
        run: |
          docker push ${{ env.DOCKERHUB_USERNAME }}/laravel_app:latest
          echo "Laravel image pushed successfully"

      - name: Push MySQL image to Docker Hub
        if: github.event_name == 'push'
        run: |
          docker push ${{ env.DOCKERHUB_USERNAME }}/mysql_app:latest
          echo "MySQL image pushed successfully"

      - name: Keep containers running for verification
        run: |
          echo "=========================================="
          echo "FINAL VERIFICATION - Stack: first-application"
          echo "=========================================="
          echo ""
          echo "=== Docker Compose Project Name ==="
          echo "Project/Stack Name: first-application"
          docker compose -p first-application config --name 2>/dev/null || echo "Using explicit project name: first-application"
          echo ""
          echo "=== Final Container Status (Stack: first-application) ==="
          docker compose -p first-application ps
          echo ""
          echo "=== Network Information (first-application_app-network) ==="
          docker network inspect first-application_app-network 2>/dev/null | grep -A 20 "Containers" || {
            echo "Checking all networks..."
            docker network ls | grep first-application
          }
          echo ""
          echo "=== Container Details ==="
          echo "Stack Name: first-application"
          echo "Network: first-application_app-network"
          echo "Volumes: first-application_mysql_data"
          echo ""
          echo "=== Verifying Stack Name in Container Labels ==="
          docker inspect laravels 2>/dev/null | grep -i "com.docker.compose.project" || echo "Checking container labels..."
          docker inspect sql_db 2>/dev/null | grep -i "com.docker.compose.project" || echo "Checking container labels..."
          docker inspect reacts 2>/dev/null | grep -i "com.docker.compose.project" || echo "Checking container labels..."
          echo ""
          echo "✓✓✓ CONFIRMED: All containers are running in stack 'first-application' ✓✓✓"

      # Cleanup step removed - containers will remain running during CI/CD execution
      # Containers run automatically with stack name "first-application" when you push code
      # - name: Cleanup containers
      #   if: always()
      #   run: |
      #     echo "Cleaning up containers from stack 'first-application'..."
      #     docker compose -p first-application down -v
      #     echo "Cleanup completed"